version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: olavurmortensen/pedgraph
    steps:
      - run:
          command: ./pedgraph/test/test.py
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
